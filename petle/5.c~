#include <stdio.h>

int main()
 {
   int i, b=6, tab[b];

     for(i=1; i<=b; i++)
     {
       scanf("%d",&tab[i]);
     }
   for(i=1; i<=b; i++)
     {
     printf("%d ",tab[i]);
     }
   printf ("\n");
   for(i=b; i>0; i--)
     {
       printf("%d ",tab[i]);
     }
     
 
return 0;
}
