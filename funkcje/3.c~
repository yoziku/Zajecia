#include <stdio.h>
#include <math.h>

void silnia ();

int main () {
  silnia();
  return 0;
}
  void silnia()
  {
  int b,a,i;
  printf("podaj liczbę naturalną do obliczenia jej silni: \n");
  scanf ("%d", &a);
  b=a;
  i=a-1;
  do { a=a*i;
    i--;}
  while (i>1);
  printf ("silnia %d to:%d \n", b,a);
 
  }
